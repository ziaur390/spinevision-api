{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/((?!assets|favicon\\.ico|logo\\.png|robots\\.txt).*)",
            "destination": "/"
        }
    ]
}